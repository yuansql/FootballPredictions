# 全表 / 多场防偷懒（V17.4.41 · 三池）

说「今天全部」「全表」「把今天场次拆完」时先读本文，再写 `01`。  
**禁止**用 `output-template.md` × 场数硬扛。禁止套 `/test-engineer` 当监督者。

## 三池（不许混成一档）

| 池 | 哪些场 | 必须写出的字面（少一条就不算走完） |
|---|---|---|
| **观察骨架** | 编号到的每一场 | 标题格式（下节）+ `排除=` + `剩余={…}` + `二次=倾斜…→` 或 `分不清→` + `方向=` + 进球数 + 【研究推荐】/【出票】各一行 |
| **重点认真拆** | ≥3 场（给 `02`） | 再加【取证清单】【情报叙事】【球队画像】比分主/次/防 + **【盲方向】**（填大段 SP 前冻结） |
| **票面池** | 【今日票面】腿（可空仓） | 文末 `【今日票面】`：预算 + 方案=空仓\|可买腿（溯源汇总表/稳健块）；**宁空仓不硬凑** |
| **可介入** | 打算真下的场 | 再加 **初盘 + 水位**（与现 SP 同屏；禁只报即时 SP） |

认真拆不够 3 场、全表却写「已按框架」→ 偷懒。骨架写了但 `排除=` 条数 ≠ 全表场数 → 没走完。  
有认真拆却无【今日票面】→ **4.41 FAIL**（允许写空仓）。

## 标题（给 lint 看的，不是给人看的排版）

每场一个 `##` 段，必须能被 `lint_draft.py` 解析：

```text
## 周四004 贝蒂斯 vs 赫塔费
```

- 小写 **`vs`**（不要只写 `VS` 当唯一标记；脚本已兼容大小写，骨架仍用小写）
- 编号 **`周` + 一二三四五六日 + 三位数字**（周一～周日都算；不是只有周五六日）
- 禁止用 `## 第一场 · 周四004 …` 这种「第 N 场」当唯一标题（可写在段内，不要替代编号行）

## 交卷闸（宣称全表完成前）

1. 数正文里的 **`排除=`**：必须等于全表场数。
2. 跑：

```bash
python3 scripts/lint_draft.py path/to/01-竞彩分析.md --day YYYY-MM-DD
```

3. **解析到 0 场却规则全绿 = FAIL**，不许说 lint 过了。有 ERROR 则 exit 1。
4. ≥`TICKET_DAY`：须有【今日票面】（可空仓）。

## 复盘分母（全表认真拆日）

写 `03-复盘.md` 时：**主 KPI = 票面池**；观察四率/方向分母 = `01`【全场汇总表】行数。禁止 silent 缩分母。  
交卷见 `references/03-复盘模板.md`（票面池在四率前）。

## 故意不跑的

- 每场四个检查脚本（结构闸 / 陷阱 / ICS / 红旗）：只在认真拆/可介入场跑，全表骨架不强制。
- `/test-product-manager` `/test-engineer` `/test-ui-reviewer`：足球分析不开。
- 已发头条的 `02`：不因补全表骨架而重发。全表是另一份研究 `01`。
