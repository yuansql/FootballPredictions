# 2足球框架 · 使用说明

仓库：https://github.com/yuansql/FootballPredictions  
版本：V17.4.2

---

## 一句话先搞清

| 东西 | 干什么 |
|------|--------|
| 根目录 `*.txt` | **你日常改规则的源文件** |
| `skills/football-predict-v17/` | **给别人 / `npx skills` 用的技能包** |
| `skills/.../references/` | 技能包里的框架副本（别人装完靠它读规则） |
| `scripts/sync-skill-bundle.sh` | **把根目录源文件复制进 references**（发布前必跑） |
| `一键投喂_全量合并.txt` | 给千问/DeepSeek 整包上传用 |

你改的是根目录；别人拉到的是 `skills/`。两边不同步就会「你本地已改、GitHub 技能包还是旧的」。

---

## `scripts/sync-skill-bundle.sh` 是干啥的？

**作用：维护用同步脚本，不是预测脚本。**

每次你改完根目录框架文件后，跑它会：

1. 执行 `python3 rebuild_一键投喂.py` → 重建 `一键投喂_全量合并.txt`
2. 把下列文件 **复制** 到 `skills/football-predict-v17/references/`：
   - 外部模型启动卡、球赛预测框架、初始框架、p_model手算  
   - 投注分析专家、小联赛、五大联赛、世界杯  
   - 完整样例、说明书、一键投喂、rebuild 脚本  

**默认不会**装到本机 `~/.cursor/skills`（你要求本机不装）。

### 怎么跑

在仓库根目录：

```bash
# 只同步到 skills/.../references（推荐，发布前）
bash scripts/sync-skill-bundle.sh

# 可选：同时装到本机 Cursor（一般不用）
bash scripts/sync-skill-bundle.sh --local-cursor
```

成功会看到类似：

```text
OK synced → .../skills/football-predict-v17/references（未安装本机 Cursor；…）
```

### 什么时候必须跑

- 改了任意根目录 `.txt` 规则  
- 准备 `git push` 给别人用之前  
- 改完想更新「一键投喂」合并包时  

### 什么时候不用跑

- 只是自己用根目录文件预测、不发 GitHub  
- 只改了 `skills/.../SKILL.md` 文案（改完仍建议 sync 一次再 push）  

### 卸载本机 Cursor skill（若曾加过 `--local-cursor`）

```bash
rm -rf ~/.cursor/skills/football-predict-v17
rm -rf .cursor/skills/football-predict-v17
```

---

## 别人怎么用（拉取）

### A. Skills CLI（推荐）

```bash
npx skills add yuansql/FootballPredictions --skill football-predict-v17
# 全局：
npx skills add yuansql/FootballPredictions --skill football-predict-v17 -g
```

### B. Git 克隆

```bash
git clone https://github.com/yuansql/FootballPredictions.git
cd FootballPredictions
```

### C. 千问 / DeepSeek

上传 `一键投喂_全量合并.txt`（根目录或 `skills/.../references/` 里同名文件），用户消息带激活句，例如：

```text
按《一键投喂》执行。本场激活：ALWAYS + 小联赛插件。五大/世界杯分卷本场禁用。
体彩默认。对阵：___ vs ___
让球(0) SP：_ / _ / _
必须先【取证清单】再【硬闸自检】【结果预测】【出票】【投注星级】。
```

| 联赛 | 激活 |
|------|------|
| 挪超/芬超/瑞超等 | ALWAYS + 小联赛 |
| 英超等五大 | ALWAYS + 五大联赛 |
| 世界杯 | ALWAYS + 杯赛 |

### D. OpenSkills

```bash
openskills install yuansql/FootballPredictions
```

---

## 你自己维护 / 发布流程

```bash
# 1. 改根目录规则（球赛预测框架.txt 等）
# 2. 同步进技能包
bash scripts/sync-skill-bundle.sh
# 3. 提交推送
git add -A
git commit -m "update framework"
git push
```

仓库需 **Public**，别人才能 `npx skills add` 到。

---

## 目录对照

```text
2足球框架 / FootballPredictions/
├── 使用.md                          ← 本文件
├── README.md                        ← GitHub 首页简介
├── scripts/
│   └── sync-skill-bundle.sh         ← 源文件 → references 同步
├── rebuild_一键投喂.py
├── *.txt                            ← 源规则（你改这里）
├── 一键投喂_全量合并.txt
└── skills/
    └── football-predict-v17/        ← 对外技能入口
        ├── SKILL.md
        ├── output-template.md
        └── references/              ← sync 脚本生成的副本
```

---

## 预测时硬要求（摘要）

1. 先搜网填【取证清单】，无清单 = 废稿  
2. RAW 联赛走 λ′ + Edge_eff≥8%；有 Understat 走 npxG、≥5%  
3. 不荐也要【结果预测】；禁止弃单后改推大球/半全场当主推  
4. 未写「北单」→ 体彩默认  

细则见 `球赛预测框架.txt` / `外部模型启动卡.txt`。

---

## 免责声明

仅供研究学习，不构成投注建议。
