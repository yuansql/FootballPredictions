# 2足球框架 · 使用说明

仓库：https://github.com/yuansql/FootballPredictions  
版本：**V17.4.9**（INTEL_FIRST 精简包 + 双轨推荐 + 双闸门 + 降维 + V15.6 五补丁）

---

## 一句话先搞清

| 东西 | 干什么 |
|------|--------|
| 根目录 `*.txt` | **你日常改规则的源文件**（精简后仅 8 个正文 + rules） |
| `rules/V15.6_patches.*` | **V15.6 五补丁**（py=可执行，txt=规范） |
| `skills/football-predict-v17/` | **给别人 / `npx skills` 用的技能包** |
| `skills/.../references/` | 技能包里的框架副本 |
| `scripts/sync-skill-bundle.sh` | **把根目录源文件复制进 references**（发布前必跑） |

**已废除（防噪音，勿再创建）：**  
`一键投喂_全量合并.txt` · `rebuild_一键投喂.py` · `初始框架.txt` · `预测框架说明书.txt`  
BLOWOUT / 禁默认 1-1 等底盘规则在 `球赛预测框架.txt`。

---

## 精简包清单（skill 只认这些）

**每场 ALWAYS：** 外部模型启动卡 · 球赛预测框架 · p_model手算 · 投注分析专家 · rules/V15.6  

**按联赛 WHEN（同场最多 1 个）：** 小联赛数据 / 五大联赛分析 / 世界杯  

**对照可选：** 完整样例_体彩默认 · output-template.md  

---

## `scripts/sync-skill-bundle.sh`

**作用：维护用同步脚本，不是预测脚本。**

改完根目录框架文件后跑：

```bash
bash scripts/sync-skill-bundle.sh
# 可选装本机 Cursor：
bash scripts/sync-skill-bundle.sh --local-cursor
```

会把上表 ALWAYS+WHEN+样例复制进 `skills/.../references/`，并清理旧噪音文件名。  
**不再**调用 `rebuild_一键投喂.py`（已删除）。

### 什么时候必须跑

- 改了任意根目录 `.txt` 或 `rules/`  
- 准备 `git push` 给别人用之前  

### 卸载本机 Cursor skill

```bash
rm -rf ~/.cursor/skills/football-predict-v17
rm -rf .cursor/skills/football-predict-v17
```

---

## 别人怎么用

### A. Skills CLI（推荐）

```bash
npx skills add yuansql/FootballPredictions --skill football-predict-v17
```

### B. Git 克隆

```bash
git clone https://github.com/yuansql/FootballPredictions.git
cd FootballPredictions
```

### C. 千问 / DeepSeek（精简分贴）

按联赛只上传/粘贴：启动卡 + 主控 + **一个**插件 + p_model + 人设。  
激活句示例：

```text
【系统】先遵守《外部模型启动卡》，只使用已贴文件。
本场：瑞超 → 已贴主控/小联赛/手算/人设。
体彩默认。对阵：___ vs ___
必须：取证→情报→球队→剧本→硬闸→结果预测→出票。
```

| 联赛 | 插件 |
|------|------|
| 挪超/芬超/瑞超等 | 小联赛 |
| 英超等五大 | 五大联赛 |
| 世界杯 | 世界杯 |

### D. OpenSkills

```bash
openskills install yuansql/FootballPredictions
```

---

## 你自己维护 / 发布流程

```bash
# 1. 改根目录规则（球赛预测框架.txt / rules/ 等）
# 2. 同步进技能包
bash scripts/sync-skill-bundle.sh
# 3. 结构性验收
python3 scripts/verify_intel_first.py
# 4. 提交推送（需你明确要求才由 Agent 执行）
```

---

## 目录对照

```text
FootballPredictions/
├── 使用.md
├── README.md
├── rules/                           ← V15.6 补丁
├── scripts/
│   ├── sync-skill-bundle.sh
│   └── verify_intel_first.py
├── 外部模型启动卡.txt
├── 球赛预测框架.txt
├── p_model手算.txt
├── 投注分析专家_人设提示词.txt
├── 小联赛数据.txt
├── 五大联赛分析.txt
├── 世界杯.txt
├── 完整样例_体彩默认.txt
└── skills/football-predict-v17/
    ├── SKILL.md
    ├── output-template.md
    └── references/                  ← sync 生成的精简副本
```

---

## 预测时硬要求（摘要）

1. **INTEL_FIRST**：取证 → 情报 → 球队 → 剧本；盘口只做出票辅助  
2. **双轨推荐（V17.4.9）**：【研究推荐】五星必含胜平负/进球数/比分（空仓也出）；【出票】可全不荐  
3. **双闸门**：胜平负 Edge_eff≥1%；否则降维让球/进球≥3%  
4. **V15.6 五补丁**每场必扫；Edge_Refine 不得改写结果预测  
5. 负 Edge / 降维与剧本打架 → **出票不荐**，仍交【研究推荐】三件套  
6. 未写「北单」→ 体彩默认  
7. 体彩 Edge 多数为负属正常；靠纪律空仓，不靠场场击穿官网  

细则见 `球赛预测框架.txt` / `外部模型启动卡.txt` / `skills/.../SKILL.md`。

---

## 免责声明

仅供研究学习，不构成投注建议。
