# 2足球框架 · 使用说明

仓库：https://github.com/yuansql/FootballPredictions  
版本：**V17.4.38**（INTEL_FIRST + 双轨 + 排除|剩余|二次 + 汇总表权威链 + 只开让球分通道 + 冷门预防 + **让球稳健可荐**）

执行细则以根目录 `SKILL.md` 为准。本页只讲怎么装、改完怎么同步。

---

## 一句话先搞清

| 东西 | 干什么 |
|------|--------|
| `references/*.txt` | **日常改规则的源文件**（8 个正文） |
| `rules/V15.6_patches.*` | **V15.6 五补丁**（py=可执行，txt=规范） |
| `SKILL.md` / `output-template.md` | **本机与仓共用的执行器 / 输出模板** |
| `skills/football-predict-v17/` | **给别人 / `npx skills` 用的技能包** |
| `scripts/sync-skill-bundle.sh` | **源文件 → references + 本机 Cursor/Agents skill** |

**已废除（防噪音，勿再创建）：**  
`一键投喂_全量合并.txt` · `rebuild_一键投喂.py` · `初始框架.txt` · `预测框架说明书.txt`

---

## 精简包清单（skill 只认这些）

**每场 ALWAYS：** 外部模型启动卡 · 球赛预测框架 · p_model手算 · 投注分析专家 · rules/V15.6  

**按联赛 WHEN（同场最多 1 个）：** 小联赛数据 / 五大联赛分析 / 世界杯  

**对照可选：** 完整样例_体彩默认 · output-template.md · 03-复盘模板.md  

---

## `scripts/sync-skill-bundle.sh`

**作用：维护用同步脚本，不是预测脚本。**

改完 `references/` 框架 / `SKILL.md` / `output-template.md` / `rules/` 后**必须跑**（默认已含本机）：

```bash
bash scripts/sync-skill-bundle.sh
# 仅更新仓库 references、不动本机 Cursor：
bash scripts/sync-skill-bundle.sh --no-local
```

会把 ALWAYS+WHEN+样例 + `03-复盘模板.md` 复制进 `skills/.../references/`，并（默认）同步到：

- 仓库 `.cursor/skills/football-predict-v17/`
- 本机 `~/.cursor/skills/football-predict-v17/`
- 本机 `~/.agents/skills/football-predict-v17/`

**不覆盖** `README.md` / `使用.md`（对外说明要手改）。

### 什么时候必须跑

- 改了任意 `references/*.txt` 或 `rules/` 或 skill 正文/模板  
- 准备 `git push` 给别人用之前  
- **本机 Cursor 要用最新规则时**

### 卸载本机 Cursor skill

```bash
rm -rf ~/.cursor/skills/football-predict-v17
rm -rf ~/.agents/skills/football-predict-v17
rm -rf .cursor/skills/football-predict-v17
```

---

## 别人怎么用

### A. Skills CLI（推荐）

```bash
npx skills add yuansql/FootballPredictions --skill football-predict-v17
```

### B. Git 克隆

```bash
git clone https://github.com/yuansql/FootballPredictions.git
cd FootballPredictions
```

### C. 千问 / DeepSeek（精简分贴）

按联赛只上传/粘贴：启动卡 + 主控 + **一个**插件 + p_model + 人设。  
激活句示例：

```text
【系统】先遵守《外部模型启动卡》，只使用已贴文件。
本场：瑞超 → 已贴主控/小联赛/手算/人设。
体彩默认。对阵：___ vs ___
必须：取证→情报→球队→剧本→硬闸→结果预测→出票。
```

| 联赛 | 插件 |
|------|------|
| 挪超/芬超/瑞超等 | 小联赛 |
| 英超等五大 | 五大联赛 |
| 世界杯 | 世界杯 |

### D. OpenSkills

```bash
openskills install yuansql/FootballPredictions
```

---

## 你自己维护 / 发布流程

```bash
# 1. 改 references/ 规则 或 SKILL.md / output-template.md
# 2. 同步进技能包 + 本机
bash scripts/sync-skill-bundle.sh
# 3. 日稿 warn lint（≥2026-09-14 查排除|剩余|二次）
python3 scripts/lint_draft.py /path/to/drafts/YYYY-MM-DD --warn-only
# 4. 若改了版本号：手改 README.md 与本文件
# 5. 提交推送（需你明确要求才由 Agent 执行）
```

---

## 目录对照

```text
FootballPredictions/
├── 使用.md
├── README.md
├── SKILL.md
├── output-template.md
├── SYNC_STAMP.txt
├── references/
│   ├── 外部模型启动卡.txt
│   ├── 球赛预测框架.txt
│   ├── p_model手算.txt
│   ├── 投注分析专家_人设提示词.txt
│   ├── 小联赛数据.txt
│   ├── 五大联赛分析.txt
│   ├── 世界杯.txt
│   ├── 完整样例_体彩默认.txt
│   └── 03-复盘模板.md
├── rules/
├── scripts/sync-skill-bundle.sh
└── skills/football-predict-v17/
    ├── SKILL.md
    ├── output-template.md
    └── references/
```

---

## 预测时硬要求（摘要）

1. **INTEL_FIRST**：取证 → 情报 → 球队 → 剧本；盘口只做出票辅助  
2. **双轨推荐**：【研究推荐】方向+进球数场场必出（比分可弃权）；【出票】可全观望  
3. **方向必须给**（V17.4.22.3）：锁主/锁客/锁平/主不败/客不败；**禁止胶着、禁止空槽**  
4. **排除三步**（V17.4.27）：`排除=` → `剩余=` → `二次=倾斜…→锁*` 或 `分不清→不败｜单子腿=`  
5. **单子倾向**（V17.4.22.4）：∈允许集或不定；禁格子倒推；文末研究二串一不标仓位  
6. **双闸门**：胜平负 Edge_eff≥1%；否则降维让球/进球≥3%；Edge 不得改写剧本  
7. 未写「北单」→ 体彩默认  

细则见 `SKILL.md` / `references/球赛预测框架.txt`。

---

## 免责声明

仅供研究学习，不构成投注建议。
