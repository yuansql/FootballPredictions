---
name: football-predict-v17
description: >-
  Executes AQQ 2足球框架 V17.4.22.4 football predictions (体彩 default unless 北单).
  INTEL_FIRST slim pack + dual recommend: research 5★ (direction required;
  goals required; score if likely). Direction-first weld permit (反剧本收据) before TOP;
  RMA dual-bay; per-score Top3; receipt ±1-goal 防 bind; HT/FT opening path;
  TOP2 live fingerprint; 对外钉槽≠看槽; 一原子合同; 禁胶着; 主不败/客不败; 单子倾向; 研究二串一; analogy sign triple; 复盘三本账.
  FORCE_SEARCH, match script, λ→λ′ / V15.6 / dual Edge ticket-only.
  Use for 竞彩 fixtures, 球赛预测, 挪超/芬超/瑞超/英超/世界杯 analysis.
---

# 2足球框架 V17.4.22.4 · 情报优先执行器（精简包 · 双轨推荐）

## 总原则（最高优先）

**足球分析的乐趣与正确顺序在情报与球队，不在赔率算术。**

| 层级 | 内容 | 角色 |
|------|------|------|
| 主菜 | 伤停/战意/赛程负荷、主客形态、风格克制、H2H 定性、比赛剧本 | **定方向与比分叙事** |
| 佐料 | 让球盘口、胜平负/子盘 SP、Edge_eff、双闸门、降维 | **只决定下不下、下多大** |

禁止：先盯 SP/Edge 再倒推方向；禁止全文只剩去水算术、没有球队故事。  
冲突：本 skill > 主控 > 人设 > 插件。

### 双轨推荐（V17.4.9 · 空仓也要有推荐）

| 轨 | 字段 | 空仓时 |
|----|------|--------|
| **研究推荐** | 【研究推荐】五星 + **方向**（锁* / 主不败 / 客不败）+ **单子倾向**（双选场）+ **进球数** + **比分**（有把握才写主/次/防） | **方向+进球数场场必出**；双选须倾向或写不定；比分可弃权 |
| **出票推荐** | 【出票】可介入/不荐 + 【投注星级】 | 可全不荐（☆☆☆☆☆） |

禁止：只写「不荐」却不交方向；禁止用【投注星级】冒充【研究推荐】；禁止写胶着凑方向。  
多场日：文末加【今晚研究 TOP】（按研究星级排序，≤3；不涉及仓位）。

### 研究 TOP 软清单（V17.4.9+ · 2026-08-13 v2 · 非弃单硬闸）

排【今晚研究 TOP】前自检（**只约束研究 TOP 席位/研究星级上沿，不改【比赛剧本】方向，不做出票弃单理由**）：

1. 主胜 SP≤**1.50** 且非λ证据几乎只有「主场/名气」→ 研究星级封顶 **★★★☆☆**，**默认不进 TOP2**（可进 TOP3 或降权区）。  
2. 杯赛/欧战**次回合**且一方首回已领先 → 「客不败/管理比赛」**不得默认满星进 TOP3**；须另有伤停/形态证据，否则降权或旁注。  
3. 德比 或 首回 **0-0** 定生死 → **默认降权出 TOP**（宁缺毋滥，勿硬凑满 3 席）。  
4. 杯赛/解放者杯/欧战资格 **首回合主场** → **默认不进 TOP1**；进 TOP2 须剧本**硬写防平**（1-1/0-0）。  
5. **刚大胜/屠杀**后短期复赛（近 1 场净胜≥4 或公开屠杀叙事）→ **不满星 TOP1**（可旁注/降权）。  
6. 欧战/杯赛拟进 TOP 前须显式 **90′ 自检**：晋级故事/加时/点球 **≠** 竞彩胜平负三点。  
7. **continuation_guest**（同址七日客队刚赢再战）：默认研究星封顶 **★★★☆☆**、**不进 TOP2**；进 TOP3 须客上轮净胜≤1 **且** 主核心缺阵≥2。禁止把「主场讨债」默认焊成 TOP1。

**噪点纪律**：单次偶然 MISS 不进清单；须同类复盘 MISS≥2 **或**结构性口径错误（如 90′）才加条。禁止把软清单升成出票弃单/改剧本方向。禁止叠「揭幕日」「心情分」等模糊条。

来源：08-05/06 初版；08-10～12 头条复盘补 4–6（普拉滕斯首回平、佐加顿斯屠杀复赛、博德 90′）。

### 方向优先 · 反剧本收据（V17.4.15 · 2026-08-26 · ADHD/08-25 复盘）

**优化序**：先把胜平负方向焊对，再谈比分篮宽。比分纪律（下节）不替代本条。

写【比赛剧本】**方向**时，若使用下列叙事标签之一焊成**唯一主轴**（或据此冲 TOP1/TOP2），必须先交**【反剧本收据】**，否则方向降为 **主不败或客不败**（须选一边），研究星封顶 **★★★☆☆**，**不得进 TOP2**：

| 标签 | 典型焊法（须收据） |
|------|-------------------|
| `revenge_home` | 主场讨债 / 同址刚输要面子 |
| `weld_draw` | 势均→焊死平局三点 |
| `manage_tie` | 领先方管理比赛 / 输一场仍晋级（90′） |
| `derby_caution` | 德比/揭幕只写谨慎闷 |
| `continuation_guest` | 同址客刚赢再战却焊主胜讨债（默认降权，见 TOP 软清单#7） |

**【反剧本收据】必填字段**（可进 `01` 场次段或框架输出）：

1. `counter_direction`：最强反方向（主胜|平|客胜）  
2. `counter_one_liner`：反剧本一句话（须能对上取证槽，禁空话）  
3. `why_reject`：为何仍拒反剧本（伤停/战意/形态，有源）  
4. `clause_id`：授权本焊的纪律条款（如 `script#1`…`script#9` / `soft#6`）  
5. 若拒不干净（说不清反剧本为何弱）→ **不得焊死单 1X2**：方向改 **主不败或客不败**（须选一边）。**禁止写胶着。**

赛后复盘对账：`counter_hit`（其实反剧本对了）vs `weld_ok`（主轴成立）vs `weld_failure`（条款失灵）。用条款热力图改 margin，**禁止**单次 MISS 直接加软清单行。

来源：08-25 森林讨债 / 瓦伦焊平；ADHD 短名单「反剧本收据」。

### RMA 双仓返修（V17.4.16 · 2026-08-26 · ADHD）

**目的**：方向准确率与三比分命中率分开归因，禁止一场 miss 混写两仓、禁止单场加 soft 条。

赛后写 `03-复盘.md` 时，每场必须先填 **RMA 路由**（模板见 `references/03-复盘模板.md`；可跑 `scripts/rma_route.py`）：

| 脚本 | 用途 |
|------|------|
| `scripts/rma_route.py` | 单场 RMA 路由 |
| `scripts/replay_rma_stats.py` | 批量读 `03-复盘` 对照表 → 方向率/比分率/RMA |
| `scripts/lint_draft.py` | 01/03 发前 warn lint |

| RMA | 条件 | 只答 |
|-----|------|------|
| `direction_rework` | 1X2 方向 miss | 三问之 **① 方向为啥对/错**；禁展开「若方向对了比分…」 |
| `score_rework` | 方向对、精确分未挂进主/次/防/旁 | 三问之 **② 比分为啥挂/没挂**；禁重审方向 |
| `closed` | 方向对且篮内命中 | 三问可简写 |
| `skip` | 遗留空槽 / claim=∅ | **不进对外分母**（仅旧稿）；4.22.3 新稿禁止空槽 |

**对外对账**走 `route_rma_public` / `public_allowed_set`（锁* / 主不败 / 客不败；遗留空槽=`skip`）。`01` 与 `02` 方向口径同一套原子。  
**覆盖**：`球赛预测框架.txt` DIRECTION_FIRST 已对齐本条；头条 `01`/`02` 仍以本 skill 为准。  
**方向仓** miss 且触焊标签 → 子标签 `counter_hit` / `weld_ok` / `weld_failure`；写入 `data/miss_fingerprint_ledger.jsonl`（供 shadow 闸，**不**因此加 soft 行）。  
**比分仓** miss → 先标 `score_miss_class`（`in_contract_pack` / `contract_out_blowout`）；合同外不改条款。合同内只记纪律#2/#9 等 **score 热力**，不改 direction 条款。  
≥2 同 clause 热力 **或** 结构性 90′ 口径错 → 才提案改 skill（一仓一刀）。

可跑 `scripts/lint_draft.py <日夹|01|03> --warn-only` 做发前/复盘 warn。

### 对外钉槽 · 类比符号 · 三本账（V17.4.21 · 2026-08-28 · ADHD）

**不是软清单新条。** 08-27 认真三场方向 1/3：可校准热门被踢出镜头后仍「钉三场」；`并列` 曾被脚本扩成三向。

1. **看槽 ≠ 钉槽（头条 02）**：认真拆 ≥3 是骨架（环境维/上下半场/进球点）。「今晚我钉 K 场」只许抄未标「不进 TOP2 / 别拿主胜凑席」且能写清 90′ 方向的场；**K 可为 0–2**。`01` 已宁缺 TOP2 → 禁止 `02`「今晚我钉三场：A、B、C」。权威仓内 `toutiao-publish.md`。  
2. **胶着不得对外收窄**（**已被 4.22.3 废止**）：旧稿 `01` 胶着三向 scout。新稿 **禁止写胶着**；用主不败/客不败。  
3. **类比符号三元组**：写「里昂课 / 结构同类」须 `(追分|领先) × (主|客) × 分差`；符号不一致禁止调用该课。`02` 标题晋级主语不得复制进 90′ 方向行。  
4. **RMA 对外入口**：对账方向喂 `02` 句面 / `public_allowed_set`。`normalize_direction`：**并列 ≠ 三向**；**主不败 ≠ 三向**。  
5. **三本账**：`03` 若写命中率分数，必须同屏 **对外三场 / 01篮 / 全表**。禁只甩 1/3。口头先报真 miss。

脚本：`rma_route.py` · `lint_draft.py`（日夹 ≥2026-08-28 钉槽/符号；≥2026-08-27 三本账；≥2026-08-31 一原子；4.22.1 空槽/句面单例/主胜防平收据）。

### 对外一原子 · 空槽 · 旁边升锁（V17.4.22.1 · 2026-08-31 · ADHD）

**不是软清单新条。** 08-27～30 对外方向 5/12、精确 2/12。一原子把账变诚实；默认锁平不抬命中。格子族 {零封,两球块,乱战} **降级硬闸**（与 `ANTI_SMALL_WIN` / `TRUE_00` 打架；热刺 0-2 合法对是 `0-1/0-2`）。

1. **一原子合同**：锁单 1X2 时，两格必须同一 1X2。**主不败**篮可同时含主胜格+平格；**客不败**篮可含客胜格+平格；禁止第三向进公开格。钉槽「并列」只并所写两边。  
2. **空槽 ≠ 锁平**（**4.22.3 起新稿禁止空槽**）：旧稿无 lean 时 `02` 空槽不进分母。新稿认真拆的场 **必须给方向**。  
3. **锁平许可证**：须含 **0-0** 或句面 `闷平` / `TRUE_00` / `死盒` / `焊平`。犹豫不是平。  
4. **旁边升锁或划场**：禁止「放旁边」当第三向。  
5. **主胜防平须收据**：`01` 方向/研究行 **主胜（防平）** 且页上有客胜偷球格（旁/path `0-1`/`1-2`）→ 须活【反剧本收据】。主不败/客不败不触发本条。  
6. **01 与 02 同向**：低结构 `01` 仍可交权重 Top3（4.22.2）；`02` 必须写出方向原子（锁* / 主不败 / 客不败）。

脚本：`atoms_from_scores` · `public_allowed_set` · `surface_1x2_tokens` · `lint_unpinned_home_weld` · `lint_02_atom_text`（≥2026-08-31）。

### 记账 skip · 合同外 miss · 低结构第三格（V17.4.22.2 · 2026-09-01）

**不是软清单新条，不改出票闸。** 4.22.1 把账做诚实；准度不靠再堆条款。08-31 空槽是闸后首夜天气。

1. **空槽 skip**：句面含「空槽」且无锁 → `public_allowed_set=∅`（**不理 01 篮**）。`route_rma_public` 返回 `skip`：不进对外分母，禁止打成 `direction_rework`。  
2. **合同外 miss**：实战净胜 **≥4** 且公开篮无净≥3 格 → `contract_out_blowout`。旁注可点名 0-3/0-4；**禁止**因此改 SCORE_PICK / ε / 加第四格 / 加 soft。`in_contract_pack`（如权重 #3 的 0-1/1-0 没装箱）才进比分返修。  
3. **低结构仍交权重 Top3**：ε 只禁进 TOP2、禁焊死第四故事。`01` **默认交满单格 #1/#2/#3**（可弃权）；第三格必须是权重 #3（含同权并列档）。屠杀不占公开三格。  
4. **闸后首夜**：新闸第一公开夜的空槽不得提案改 skill；记者撒谎（假 miss）只改脚本。

### 方向必须给 · 禁胶着 · 比分可弃权（V17.4.22.3 · 2026-09-02）

**不是软清单新条，不改出票闸。** 胶着三向永远命中，不是推荐。亲爱的点名：方向必须给；可写主不败/客不败；禁用胶着；比分没把握只给进球数，比分中的可能性大必须给。

1. **方向原子（`01`/`02` 同口径）**：每场认真拆必须写且只写其一——`锁主` / `锁客` / `锁平` / `主不败` / `客不败`（单选也可写 `主胜`/`客胜`/`平局`，等同锁）。**禁止胶着。禁止空槽。禁止「方向空着」。**  
2. **允许集**：锁主=`{主胜}`；锁客=`{客胜}`；锁平=`{平}`；主不败=`{主胜,平}`；客不败=`{客胜,平}`。主不败打出客胜=方向 miss；客不败打出主胜=方向 miss。  
3. **比分**：有把握 → 必须给主/次/防（旁可选）。没把握 → **比分弃权**，必须写进球数。无比分篮且方向命中 → 比分仓 `closed`（不计比分 miss）。  
4. **进球数**：场场必出（研究行）。  
5. **日闸**：`lint_draft.py` `DIR_MUST_DAY=2026-09-02`；此前旧稿空槽/胶着仍按 4.22.2 读。

脚本：`normalize_direction` · `public_allowed_set` · `lint_02_must_direction` · `score_miss_class` · `RmaRoute.SKIP`。

### 单子倾向 · 装箱 · 研究二串一（V17.4.22.4 · 2026-09-04）

**不是软清单新条，不改出票闸，不推翻 4.22.3 方向原子。** 主不败命中 ≠ 胜平负单子赚钱。因果硬：**允许集 → 单子倾向 → 再装箱**。禁止用主推比分的 1X2 倒推倾向（ε 内 #1/#2 差 0.02 不是锁）。

1. **方向原子仍是** 锁* / 主不败 / 客不败。`02` 先写方向行，**下一句**才写单子腿。禁止把倾向写成锁主/锁客/锁平。锁* 场倾向可省略（等同所锁那一格）。  
2. **单子倾向**：在允许集内用剧本/收据选 `主胜` / `平` / `客胜`。分不清 → 写 **单子腿不定**。禁止用格子权重反推。倾向必须 ∈ 允许集（主不败不得倾向客胜；客不败不得倾向主胜）。  
3. **装箱（倾向已定）**：主/次 = 倾向那一族；防 = 允许集另一向（多半是平）。仍低结构、禁 TOP2。三格从已有权重池**重排**，不另发明第四格。`02` **只写主/次/防**；旁挂只留 `01`；禁止「备 1-0」当第四比分（上半场「备」=路径）。  
4. **装箱（单子腿不定）**：维持 4.22.2 权重 Top3 混装。  
5. **进球双选**：从本场已写进球档收成相邻两档（一至二球→1/2；一至三球按三格总球砍成 1/2 或 2/3）。禁止另开进球剧本。  
6. **文末【研究二串一】**（`02` 全文最后、钉槽/编号之后）：进球双选 × 胜平负（用单子倾向那一格；不定则该场不拼胜平负腿）。研究向，**不标仓位**。出票默认仍不荐。  
7. **RMA**：方向分母仍按双选允许集。另记「单子腿」命中。禁止拿倾向刷方向 x/n。  
8. **日闸**：`lint_draft.py` `LEAN_DAY=2026-09-04`；此前旧稿不回溯。

脚本：`lint_02_lean` · `_lint_lean_pack`。

### 比分推荐 · 单格 Top3（V17.4.20 · ADHD）

**顺序硬**：双方分析/剧本 → **方向锁** → **进球档** → 写 **【情景路径】**（2–4 条，`weight`+终场叶）→ **每个比分单独计权** → 取权重 **Top3 单格** → 公开 **≤3**。  
**禁止**把三格当套餐。每个比分（2-1、1-0、2-0、1-1…）**独立**计权。  
第3与第4单格权重差 **&lt;ε（默认 0.15）** → **低结构**（禁进 TOP2；`01` 仍默认交权重 Top3，第三格= #3 或弃权）。

| 结构档 | 何时 | 装箱 |
|--------|------|------|
| **低结构** | 主不败/客不败、收据拒不干净、**ε**、无焊唯一主轴 | 单子腿不定 → 主/次/防 = 权重 #1/#2/#3；倾向已定 → **先按族重排**（主/次=倾向族，防=允许集另一向，见 4.22.4）；逃生异族写 **旁注**（只留 `01`）；**不得进 TOP2** |
| **高结构** | 方向焊清、收据过闸、#3-#4 margin≥ε | 主/次 = 焊轴族单格 #1/#2；**防** = 平防 **或** 收据 ±1 球反方向（见下） |

**收据防格（V17.4.20 · 比分仓）**：【反剧本收据】过闸且 `counter_direction` 与焊轴**异号**时，须有一条情景叶 `weight≥τ（0.10）` 的 **±1 球**反方向分（主胜焊→**0-1**；客胜焊→**1-0**），占用公开 **防**。02 可写「真爆冷最像 0-1」。屠杀/多球反方向仍旁注。主+次不得混异族。  
**同链反回声**：情景叶与公开格全是同一胜族、无 1-1/反方向 → 视为双计，须换防格。  
**HT/FT 分轨**：`ht_path_A/B`（半场态）与 `path_A/B/C`（90′ 终场）分开写。`derby_caution` / `weld_draw` / 谨慎闷 → 必须有 **开口/对方先破门** 备路径（#8）。02 只留「上半场主/备」两句。  
**TOP2 预闸**：低结构不得进 TOP2；焊标签须 `clause_id` 绑 path 叶；同指纹 **1 次** `counter_hit` → live 禁 TOP2（不等 ≥2）。  
**禁止**：无路径任选；三格套餐。  
**旁注**：#9 屠杀/多球逃生；收据未过闸时的异族 ±1 球。收据过闸后 ±1 球进 **防**，进 `02`。  
**废止**：邻格主闸（V17.4.17）；同等合法/两簇任选（V17.4.18）；三格套餐（V17.4.19 初版）。  
**交卷口吻**：`02` 球迷话；weight / τ / margin 留 `01`。  
脚本：`score_geometry.py`（`pick_counter_leaf` / `lint_anti_echo`）· `lint_draft.py` · `receipt_fingerprint.counter_hit_shadow_check`。

### 收据指纹 · shadow 闸（V17.4.16 · ADHD；TOP2 live · V17.4.20）

结构化指纹 `{weld_tag, clause_id, counter_direction, intel_slot_shape}`——禁自由文本聚类。  
近 **N=10** 条方向 miss 同指纹 **≥2** → **shadow 警告**（暂不硬拦全场）。  
**TOP2**：同指纹且 `subtag/note` 含 **counter_hit** **≥1** → **live 禁 TOP2**。  
脚本：`scripts/receipt_fingerprint.py` + `data/miss_fingerprint_ledger.jsonl`。单场 miss **仍不加** soft 清单行。

### 剧本 / 比分推荐纪律（V17.4.20 · 2026-08-27 · 球迷+分析师准确率闸）

写【比赛剧本】与【研究推荐】比分时（**改推荐质量，不做出票弃单理由；单场 MISS 不加软清单条**）：方向未过「反剧本收据」闸前，**禁止**先扩比分篮假装完成。

1. **环境维不焊方向**：天气/场地/湿度等只加权，**禁止单独焊死**胜平负方向。中游主场打争冠/榜眼客：须并列写清 **「主队先破门 → 主胜或平」** 路径，不得只留客胜主轴。  
2. **逃生口**：收据**未**过闸 → 相反 ±1 球只写 **旁注**。收据**已**过闸 → 该 ±1 球占公开 **防格**（τ≥0.10）；多球/屠杀仍旁注。开口平局研究侧可旁挂 **2-2**。  
3. **上下半场分轨**：HT 与 FT 分开写；HT 主轴与终场方向同号时必须有 **对方先破门** 备路径。比分三格对双方路径，不是几何凑邻。  
4. **进行中大比分 ≠ 终场**（08-15 米亚尔比 0-3→4-3）：上半场屠杀成立时，下半场仍须保留 **落后方追分/翻盘** 终局；禁止用半场比分锁死 90′ 方向推荐。  
5. **体能税不焊小胜上限**（08-15 威廉二世 1-4）：中周 120′/短间隔只加权；纸面明显优时 **旁注** 留大胜（如 1-4 / 0-3）。  
6. **揭幕/小球教练画像不焊平**（08-15 阿拉维斯 3-0）：揭幕战不得单独焊死平/小球；主场揭幕须并列 **主胜多球** 旁注。  
7. **领先两球 ≠ 终场锁定**（08-16 阿贾克斯 2-0→2-2、桑坦德 2-0→2-2）：研究侧可旁挂 **2-2**；公开三格按单格权重（可含 1-1 防平）。  
8. **双新帅揭幕 / 同城德比不焊「谨慎闷半场」**（08-24 富勒姆 2-3 切尔西）：须并列 **「开场见球 / 开口」**（`ht_path_B`）。开口篮旁注可挂 **2-3 / 2-2**。  
9. **须屠三球的次回合翻盘 · 旁挂多球**（08-25 林茨 4-1 凯尔特人 90′）：须净胜≥3 时 **旁注** 挂 **3-1 / 4-1**；公开 Top3 若分析把屠杀分排进前三则可进格。加时/点球≠竞彩 90′。  
10. **双方分析 → 单格 Top3**（V17.4.19+ / 4.22.2）：每个终场分单独计权；**禁止**邻格/套餐。margin&lt;ε → 低结构（仍交 #3，禁 TOP2）。  
11. **同链反回声**（V17.4.20）：伤停/形态与球迷 anecdote 不得当两条独立信号把三格全焊成同一胜族；防格须换异链路（平或收据反方向 ±1）。  
12. **continuation_guest**（V17.4.20）：同址客刚赢再战，禁止默认复仇 TOP1（见软清单#7）。

来源：`toutiao/drafts/2026-08-14`～`08-25` 复盘；`2026-08-26/27` ADHD（单格计权 → 球迷+分析师准确率闸）。

### 复盘 → skill 闭环（同日）

头条/研究日夹写 `03-复盘.md` 须：**先填 RMA 路由表** → 再答三问（方向 / 比分 / 剧本；**按仓只展开对应问**）。模板 `references/03-复盘模板.md`。  
对外空槽仅旧稿标 `skip`。新稿方向必给，进分母。比分 miss 先写 `score_miss_class`：合同外 **不改本 skill**。  
**结构性病灶**才改本 skill（或软清单）；改后跑 `scripts/sync-skill-bundle.sh` + `python3 scripts/verify_accuracy_hooks.py`。终点是优化本执行器，不是交命中成绩单。

### Edge 诚实声明（体彩）

体彩官方 SP 抽水厚，RAW 口径再打折后，**多数场次 Edge 为负属正常**。  
普通玩家「赢」靠：**情报定剧本 + 多数空仓 + 闸门真开才出手**；不靠场场击穿官网定价。  
负 Edge → 【出票】不荐；**仍必须交方向 + 进球数**（比分有把握才交）+ 情报/剧本。

## ROOT（便携）

1. `SKILL_DIR/references/`（若有 `球赛预测框架.txt`）  
2. 否则仓库根目录  
3. 或环境变量 `AQQ_FOOTBALL_ROOT`  

禁止 `原足球框架`。禁止加载已删除的噪音包（一键全量合并 / 说明书 / 初始框架）。

## 精简包文件（只这些）

| 角色 | 文件 | 何时读 |
|------|------|--------|
| 硬闸 | `外部模型启动卡.txt` | 每次 |
| 主控 | `球赛预测框架.txt`（含 INTEL_FIRST / BLOWOUT / 双闸） | 每次 |
| 手算 | `p_model手算.txt`（§5b·§5c） | 每次（算出票前） |
| 人设 | `投注分析专家_人设提示词.txt` | 每次 |
| 补丁 | `rules/V15.6_patches.txt` + `rules/V15.6_patches.py` | 每次 |
| 模板 | `output-template.md` | 每次对照输出 |
| 复盘 | `03-复盘模板.md` | 写 `03-复盘.md` 时 |
| 样例段 | toutiao `drafts/_templates/01-竞彩分析-V17.4.20-样例段.md` | 01 收据防格+ht_path |
| 样例 | `完整样例_体彩默认.txt` | 对照格式时 |
| Tier3 | `小联赛数据.txt` | **仅**挪超/芬超/瑞超等无 Understat |
| 五大 | `五大联赛分析.txt` | **仅**英西德意法 |
| 杯赛 | `世界杯.txt` | **仅**世界杯/欧洲杯决赛圈 |

**同场最多 1 个联赛插件。** 未命中联赛类型的插件禁止打开。

## 作业流（强制顺序）

1. **WebSearch →【取证清单】**（关键槽≥2 才可介入；含积分/近5/伤停/H2H）  
2. **【情报叙事】**：伤停·停赛·战意·赛程密度·主客场因素（有源才写，禁编）  
3. **【球队画像】**：主队 / 客队各一段（形态、火力/防守、风格；写清克制或互咬）  
4. **【比赛剧本】**：先定方向（命中讨债/焊平/管理晋级/德比谨慎标签 → 先交**【反剧本收据】**；拒不干净 → 主不败/客不败，禁胶着）→ **允许集内写单子倾向**（分不清则不定，禁格子倒推）→ 净胜档 → **进球档** → 有把握才 **情景路径 + 按倾向装箱** → `ht_path` 开口备路径；遵守「方向优先」与「V17.4.20」+「V17.4.21」+「V17.4.22.1」+「V17.4.22.2」+「V17.4.22.3 方向必须给」+「V17.4.22.4 单子倾向/研究二串一」  
5. **盘口辅助**：读让球线与 SP → λ（RAW→λ′）→ StarFactor(子盘) → 泊松 → Kryptonite/Jinx  
6. **出票闸门**：胜平负 Edge_eff；≥**1%** → 通道=胜平负；**<1%** 且未硬弃 → **降维**让球→进球（≥**3%**；≥5% 可标单关高推）  
7. **Zombie / Edge_Refine** → 【出票】【投注星级】（出票星；降维≤★★★☆☆）+ **【研究推荐】**（研究星 + 胜平负/进球数/比分）  

硬弃单 → 【出票】不荐；**仍必须【研究推荐】三件套 + 情报+剧本+结果预测**。

## 双闸门（出票用 · 非方向用）

| 通道 | Edge_eff 闸门 | 实战含义 |
|------|---------------|----------|
| 胜平负 | **≥ 1%** | 击穿体彩抽水后的可介入门槛 |
| 让球 / 进球数 | **≥ 3%** | 子盘方差大；≥**5%** 宜标「高赔单关主推」 |

Edge **不得**改写已定剧本方向与【结果预测】。  
**Edge_Refine / MKT_CONFLICT**：最多改【出票】提示；**禁止静默覆盖【比赛剧本】【结果预测】**。  
降维方向与结果预测打架 → **出票不荐**（禁止推打架的让球/进球）。

## V15.6 五补丁（永久 · 每场必扫）

| 补丁 | 触发 | 动作 |
|------|------|------|
| **StarFactor** | 客队传统三强 + 后防伤≥2 + 中前场全勤 + 客胜赔≤1.50 | λ_A×1.15、λ_H×1.10；**仅子盘** |
| **Kryptonite** | 客队近10交锋≥8胜 | P_A 偏移后归一化（Edge 前） |
| **Jinx** | 主场近5交锋≥3平 | P_D 6:4 平滑；若最高→平局首选+0-0/1-1 |
| **Zombie** | 主队垫底2 + 胜≤1 + 受让≥1 | 让球 Edge×0.60×0.65 + 深盘陷阱预警 |
| **Edge_Refine** | 主盘 Edge_eff≤-5% | 冷门扫描；≥3%→**仅改出票提示** |

**Pipeline：** 情报/球队/剧本 → 取证闭环 → StarFactor → 泊松 → Kryptonite/Jinx → Edge → Zombie → Edge_Refine → 出票

## 禁止

- 先算 Edge / 去水再编球队故事  
- 加载一键全量合并或无关联赛插件凑上下文  
- 胜平负无 Edge 就全弃且**不跑降维**  
- 主盘 Edge≤-5% **不跑 Edge_Refine** 仍推热门  
- 命中 Zombie 却不写「⚠️ 深盘陷阱预警」  
- 无让球/进球 SP 却口头推大球当出票  
- 降维方向与结果预测打架仍出票  
- 用统一 3% 闸门卡住本已 ≥1% 的胜平负  
- 输出缺【情报叙事】或【球队画像】却宣称「已按框架」  
- 【出票】不荐却省略方向或进球数  
- 用天气/场地单独焊死方向；或开口平比分篮无 2-2；或上下半场只留单一主路径无备路径  
- 双新帅揭幕/同城德比把「双方谨慎闷半场」焊成唯一主路径；或开口德比比分篮无 2-3/2-2  
- 须净胜≥3 的次回合翻盘局公开三格与旁注都无 3-1/4-1；或把领先方「管理比赛」写成唯一静态剧本  
- 用讨债/焊平/管理晋级/德比谨慎叙事焊死唯一方向并冲 TOP2，却无**【反剧本收据】**  
- 公开三格混主客胜族、或总球落出分析进球档；把邻格/曼哈顿当推荐理由；低结构发明非权重 #3 的第三精确分  
- `01`/`02` 写胶着，或认真拆的场空槽/方向空着  
- 用主推比分 1X2 倒推单子倾向；或把倾向写成锁主/锁客/锁平  
- 主不败倾向客胜、客不败倾向主胜；或拿单子腿命中刷方向 x/n  
- `02` 写第四比分格（「备 1-0」/旁挂）；或另开进球剧本做双选  
- 文末二串一标仓位 / 写成可介入  
- `01` 已宁缺 TOP2，`02` 仍「今晚我钉三场」枚举方向；或「钉K场」与「别拿主胜凑席」同文  
- 写里昂课/结构同类却不写符号三元组；或把标题晋级主语直接当 90′ 方向主语  
- `03` 甩命中率分数却不同屏对外三场/01篮/全表  

## 必出

【取证清单】【情报叙事】【球队画像】【比赛剧本】（含命中标签时的**【反剧本收据】**）【硬闸自检】含出票通道+【降维】+【V15.6补丁】【结果预测】**【研究推荐】**【出票】【投注星级】

详见 [output-template.md](output-template.md)。
