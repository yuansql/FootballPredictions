# 03-复盘模板（V17.4.16 · RMA 双仓 · 4.22.4 单子腿另记）

> 对照：`01-竞彩分析.md` / `02-头条正文.html`  
> **三问**：方向为啥对/错 · 比分为啥挂上/没挂 · 剧本推荐为啥准/不准  
> **RMA**：每场必须先声明返修仓，禁止混写两仓问题  
> **噪点纪律**：单次 MISS 不加软清单；≥2 同构或结构性口径错才改 skill

---

## 先说系统改了什么

| 场 | 表象 | RMA | 判 |
|---|---|---|---|
| … | … | `direction_rework` \| `score_rework` \| `closed` \| `skip` | … |

**本篇决策**（只写结构性改 skill 的项）

| 病灶 | 改 / 不改 | 理由 |
|---|---|---|

---

## RMA 路由表（必填 · 赛后先填）

| 场 | 推方向 | 实际1X2 | 推比分篮 | 实际比分 | RMA | 方向子标签 |
|---|---|---|---|---|---|---|
| … | … | … | 主/次/防/旁 | … | … | `counter_hit` \| `weld_ok` \| `weld_failure` \| — |

**RMA 规则**（`scripts/rma_route.py`）：

- **direction_rework**：1X2 方向 miss → 只答「方向为啥对/错」，不改比分纪律  
- **score_rework**：方向对、精确分未挂 → 只答「比分为啥挂/没挂」，不重审方向  
- **closed**：方向对且篮内命中 → 三问可简写  
- **skip**：仅遗留空槽 / claim=∅ → **不进分母**（4.22.3 新稿禁止空槽）  
- **对外入口**：方向喂 `02` 句面 / `public_allowed_set`。原子：锁主/锁客/锁平/主不败/客不败。**禁用胶着。**

方向仓 miss 带焊标签时，追加写入 `data/miss_fingerprint_ledger.jsonl`（供 shadow 闸）。

---

## 三本账（写了命中率就必须同屏 · V17.4.21）

| 分母 | 方向 | 精确比分 | 口径 |
|---|---|---|---|
| **对外三场** | a/3 | b/3 | `02` 句面；有比分才计精确 |
| **01篮** | … | … | 方向与 `02` 同原子；比分含 `01` 旁挂 |
| **全表** | … | … | 对照附录；滚动窗不是合同 |

禁只甩 1/3。口头先报真 miss。

### 对外合同留样（V17.4.22.4）

| 场 | 句面锁 | claim_set | 单子倾向 | 实际1X2 | miss_class | 单子腿 |
|---|---|---|---|---|---|---|
| … | 锁平 \| 锁主 \| 锁客 \| 主不败 \| 客不败 | `{平}` / `{主胜}` / `{客胜}` / `{主胜,平}` / `{客胜,平}` | 主胜 \| 平 \| 客胜 \| 不定 | … | `A` / — | 命中 \| 穿 \| — |

- `miss_class`：`A`=允许集没有实战 1X2；方向对则 `—`（精确 miss 走 score 仓）。
- **4.22.3 新稿方向必须给**，进对外分母。旧稿空槽仍 `skip`。
- **4.22.4**：方向分母仍按双选。单子腿另记，禁止拿倾向刷方向 x/n。≥2026-09-04 双选场须有倾向或「不定」。
- 比分 miss 另标 `score_miss_class`。无比分篮且方向命中 → 比分仓 closed。
- 合同外（净≥4 且篮无净≥3）**不提案改 skill**。

---

## 对照（附录）

| TOP | 对阵 | 推方向 | 推比分 | 赛果（90′） | RMA |
|---|---|---|---|---|---|

---

## TOPn｜… · 方向·比分·剧本

（按 RMA 只展开对应仓的问题；direction_rework 禁写「若方向对了比分…」）

### ① 方向为啥对/错（仅 direction_rework / 方向相关 closed）

### ② 比分为啥挂/没挂（仅 score_rework / 比分相关 closed）

### ③ 剧本推荐为啥准/不准

**带回**：…

---

## 对 skill 的落地

1. …  
2. 改后跑 `bash scripts/sync-skill-bundle.sh` + `python3 scripts/verify_accuracy_hooks.py`

---

## 口头结论

…
